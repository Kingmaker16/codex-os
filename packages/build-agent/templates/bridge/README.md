# Codex Bridge

API gateway for Codex models.

## Endpoints
- `GET /health`
- `GET /providers`
- `POST /respond?provider=openai|deepseek&model=model-id`

## Scripts
- `npm run dev`   — run with ts-node (watch)
- `npm run build` — compile TypeScript to dist
- `npm start`     — run compiled output
